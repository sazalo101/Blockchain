import { Connection } from '@celo/connect';
import Web3 from 'web3';
export declare const getParsedSignatureOfAddress: (sha3: Web3['utils']['soliditySha3'], sign: Connection['sign'], address: string, signer: string) => Promise<{
    v: number;
    r: string;
    s: string;
}>;
